#include <bits/stdc++.h>

using namespace std;

int main()
{
    // first (id), second (website)
    pair<int, string> site(1, "www.google.com");
    cout << site.first << " " << site.second << endl;

    // A comparação entre dois pair compara primeiro o first e depois o second
    
    return 0;
}